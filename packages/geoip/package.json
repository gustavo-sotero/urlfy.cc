{
  "name": "@urlfy/geoip",
  "version": "0.0.0",
  "private": true,
  "description": "Shared GeoIP lookup with Redis caching for urlfy.cc",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "type-check": "bunx tsc --noEmit",
    "lint": "bunx biome check . --write",
    "test:unit": "bun test src/"
  },
  "dependencies": {
    "@maxmind/geoip2-node": "^7.1.0",
    "@urlfy/cache": "workspace:*",
    "@urlfy/telemetry": "workspace:*"
  },
  "devDependencies": {
    "@urlfy/config-ts": "workspace:*"
  }
}
