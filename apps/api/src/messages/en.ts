/**
 * Email messages for the API's email rendering service.
 * Only the Emails section is needed — full UI messages live in apps/web.
 */
import { Emails } from './en/emails';

const enMessages = { Emails };
export default enMessages;
export type EnMessages = typeof enMessages;
